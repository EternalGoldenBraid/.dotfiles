dataset:
  _target_: src.data.daily_dataset.EventFeatureDataset
  key: "event_features" # HDF5 key to the dataset
  root: data
  raw_dir: ${data.dataset.root}/raw
  processed_dir: ${data.dataset.root}/processed
  filename: "data_dump.h5"
  other_features: [
      # 'meditation',
      # 'rating_sleep',
      # 'rating_day',
      # 'cw', 'screen'
      ]
  shuffle: true

  train_ratio: 0.8
  val_ratio: 0.1
  test_ratio: 0.1
    

  min_tag_occurrence: 3